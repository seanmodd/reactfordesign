import styled from 'styled-components';
import {
  background,
  margin,
  opacity,
  padding,
  position,
  radius,
  size,
  type,
} from './Utils';

export const Box = styled.div`
  ${background}
  ${margin}
  ${opacity}
  ${padding}
  ${position}
  ${radius}
  ${size}
  ${type}
`;
