import styled from "styled-components";
import {
  background,
  margin,
  opacity,
  padding,
  position,
  radius,
  size,
} from "./Utils";

export const Image = styled.img`
  ${background}
  ${margin}
  ${opacity}
  ${padding}
  ${position}
  ${radius}
  ${size}
`;
